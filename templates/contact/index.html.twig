{% extends "admin/bars.html.twig" %}

{% block title %}Contact index{% endblock %}

{% block contentpage %}
    <h1>Contact List</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>OrganizationId</th>
                <th>CreatedTime</th>
                <th>Name</th>
                <th>CountryId</th>
                <th>Street</th>
                <th>CityId</th>
                <th>CityText</th>
                <th>StateId</th>
                <th>StateText</th>
                <th>ZipcodeId</th>
                <th>ZipcodeText</th>
                <th>ContactNo</th>
                <th>Phone</th>
                <th>Fax</th>
                <th>CurrencyId</th>
                <th>RegistrationNo</th>
                <th>Eam</th>
                <th>LocaleId</th>
                <th>IsCustomer</th>
                <th>IsSupplier</th>
                <th>PaymentTermsMode</th>
                <th>PaymentTermsDays</th>
                <th>AccessCode</th>
                <th>EmailAttachmentDeliveryMode</th>
                <th>IsArchived</th>
                <th>IsSalesTaxExempt</th>
                <th>DefaultExpenseProductDescription</th>
                <th>DefaultExpenseAccountId</th>
                <th>DefaultTaxRateId</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for contact in contacts %}
            <tr>
                <td><a href="{{ path('contact_edit', {'id': contact.id}) }}">{{ contact.id }}</a></td>
                <td>{{ constant('App\\Constant\\ContactTypeConstants::CHOICES')[contact.type] }}</td>
                <td>{{ contact.organizationId }}</td>
                <td>{{ contact.createdTime ? contact.createdTime|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ contact.name }}</td>
                <td>{{ contact.countryId }}</td>
                <td>{{ contact.street }}</td>
                <td>{{ contact.cityId }}</td>
                <td>{{ contact.cityText }}</td>
                <td>{{ contact.stateId }}</td>
                <td>{{ contact.stateText }}</td>
                <td>{{ contact.zipcodeId }}</td>
                <td>{{ contact.zipcodeText }}</td>
                <td>{{ contact.contactNo }}</td>
                <td>{{ contact.phone }}</td>
                <td>{{ contact.fax }}</td>
                <td>{{ contact.currencyId }}</td>
                <td>{{ contact.registrationNo }}</td>
                <td>{{ contact.eam }}</td>
                <td>{{ contact.localeId }}</td>
                <td>{{ contact.isCustomer ? 'Yes' : 'No' }}</td>
                <td>{{ contact.isSupplier ? 'Yes' : 'No' }}</td>
                <td>{{ contact.paymentTermsMode }}</td>
                <td>{{ contact.paymentTermsDays }}</td>
                <td>{{ contact.accessCode }}</td>
                <td>{{ contact.emailAttachmentDeliveryMode }}</td>
                <td>{{ contact.isArchived ? 'Yes' : 'No' }}</td>
                <td>{{ contact.isSalesTaxExempt ? 'Yes' : 'No' }}</td>
                <td>{{ contact.defaultExpenseProductDescription }}</td>
                <td>{{ contact.defaultExpenseAccountId }}</td>
                <td>{{ contact.defaultTaxRateId }}</td>
                <td>
                    <a href="{{ path('contact_show', {'id': contact.id}) }}" class="btn btn-info">show</a>
                    <a href="{{ path('contact_edit', {'id': contact.id}) }}" class="btn btn-dark">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="32">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('contact_new') }}" class="btn btn-primary">Create new</a>
{% endblock %}
